#pragma once
#include<iostream>
#include<string>
using namespace std;
bool canSucess(string& wildChar, string& input) {
	int len = strlen(wildChar.c_str());
	for (int i = 0; i < len; i++)
	{
	
	}
	return true;
}

int test_wildcardCharacter() {
	string wildChar;
	string inputStr;
	while (cin>>wildChar>>inputStr) {

	}

	return 0;
}